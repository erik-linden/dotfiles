## cuda
https://developer.nvidia.com/cuda-downloads
Follow instructions, add all .debs before installing.

## install cudnn
https://developer.nvidia.com/rdp/cudnn-download
https://docs.nvidia.com/deeplearning/sdk/cudnn-install/index.html#installlinux-tar

## chrome
https://www.google.com/chrome/
sudo dpkg -i google-chrome-stable_current_amd64.deb
sudo apt-get update
sudo apt-get install google-chrome-stable

## bunch of programs
sudo apt-get install terminator silversearcher-ag vim vim-gtk texlive-full texmaker tmux htop meld ninja-build ffmpeg vlc dconf-tools unclutter i3 numlockx feh xfce4-power-manager arandr

## conda
https://conda.io/docs/user-guide/install/linux.html

## pyton packages
conda install scipy matplotlib ipython flake8 autopep8

flake8 is needed by syntastic
autopep8 is needed by vim-autoformat

## fish
sudo apt-add-repository ppa:fish-shell/release-2
sudo apt-get update
sudo apt-get install fish

We can't set the default shell with `chsh`, since we are using LDAP.
But the dotfiles should enable fish in Terminator and tmux.

## vim-plug
curl -fLo ~/.local/share/nvim/site/autoload/plug.vim --create-dirs \
    https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim

Run PlugInstall in vim to install plugins listed in .vimrc

## powerline
sudo apt-get install fonts-powerline
sudo apt-get install powerline python3-powerline

It may be necessary to modify the argument order in the fish bindings file manually.
/usr/share/powerline/bindings/fish/powerline-setup.fish
https://groups.google.com/forum/#!topic/powerline-support/m94nK0Bmg28

## ssh
Add keys to ~/.ssh/
If server, add public key to ~/.ssh/authorized_keys

## pycharm
https://www.jetbrains.com/pycharm/download
https://confluence.tobii.intra/display/PTOOL/PyCharm

## spotify
Use Debian package
https://www.spotify.com/se/download/linux/

## dropbox
https://www.dropbox.com/install

## mendeley
https://www.mendeley.com/download-desktop/Linux/

## power settings
Disable screen turning off -> xfce4-power-manager-settings

## imagej
https://fiji.sc/#download

## p4merge
http://blogs.pdmlab.com/alexander.zeitler/articles/installing-and-configuring-p4merge-for-git-on-ubuntu/

## tobii certificate
https://artifactory.tobii.intra/artifactory/tt-toolchains/certs/Tobii-CA-ROOT-SHA256-current.cer

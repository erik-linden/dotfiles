## conda
wget https://repo.continuum.io/miniconda/Miniconda3-latest-Linux-x86_64.sh
chmod +x Miniconda3-latest-Linux-x86_64.sh
./Miniconda3-latest-Linux-x86_64.sh

## python packages
conda install scipy matplotlib ipython flake8 pylint autopep8 sympy pandas tqdm
conda install -c conda-forge powerline-status pygit2

Useful tools + vim ALE/YCM + powerline

## powerline fonts
wget https://github.com/powerline/powerline/raw/develop/font/PowerlineSymbols.otf
wget https://github.com/powerline/powerline/raw/develop/font/10-powerline-symbols.conf
mkdir -p ~/.local/share/fonts/
mv PowerlineSymbols.otf ~/.local/share/fonts/
fc-cache -vf ~/.local/share/fonts/
mkdir -p ~/.config/fontconfig/conf.d/
mv 10-powerline-symbols.conf ~/.config/fontconfig/conf.d/

## chrome
https://www.google.com/chrome/
sudo dpkg -i google-chrome-stable_current_amd64.deb
sudo apt-get update
sudo apt-get install google-chrome-stable

## fish
sudo apt-add-repository ppa:fish-shell/release-2
sudo apt update
sudo apt install fish

We can't set the default shell with `chsh`, since we are using LDAP.
But the dotfiles should enable fish in Terminator and tmux.

## vim
sudo add-apt-repository ppa:jonathonf/vim
sudo apt update
sudo apt install vim vim-gtk

This gives us vim >= 8.0.

## vim plugins
curl -fLo ~/.vim/autoload/plug.vim --create-dirs \
    https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim

Run PlugInstall in vim to install plugins listed in .vimrc

## git
sudo add-apt-repository ppa:git-core/ppa
sudo apt update
sudo apt install git

cd /usr/share/doc/git/contrib/diff-highlight/
sudo make

## bunch of programs
sudo apt install \
    arandr \
    cheese \
    cifs-utils \
    cmake \
    curl \
    default-jdk \
    feh \
    ffmpeg \
    htop \
    i3 \
    i3lock \
    inkscape \
    jabref \
    keepass2 \
    libreoffice \
    meld \
    ninja-build \
    numlockx \
    openssh-server \
    pinta \
    screen \
    shutter \
    silversearcher-ag \
    smbclient \
    terminator \
    texlive-full \
    texmaker \
    tmux \
    tree \
    unclutter \
    v4l-utils \
    vlc \
    xfce4-power-manager

## cuda
sudo rm /etc/apt/sources.list.d/cuda*
sudo apt remove nvidia-cuda-toolkit
sudo apt remove nvidia-*
sudo apt autoremove
sudo apt update

sudo apt-key adv --fetch-keys  http://developer.download.nvidia.com/compute/cuda/repos/ubuntu1604/x86_64/7fa2af80.pub
sudo bash -c 'echo "deb http://developer.download.nvidia.com/compute/cuda/repos/ubuntu1604/x86_64 /" > /etc/apt/sources.list.d/cuda.list'
sudo apt update
sudo apt install cuda

## ssh
Add keys to ~/.ssh/
If server, add public key to ~/.ssh/authorized_keys

## pycharm
https://www.jetbrains.com/pycharm/download
https://confluence.tobii.intra/display/PTOOL/PyCharm

## spotify
Use Debian package
https://www.spotify.com/se/download/linux/

## dropbox
https://www.dropbox.com/install

## mendeley
https://www.mendeley.com/download-desktop/Linux/

## i3
Nautilus will try to manage desktop icons, and then crash things. Google for
gnome settings to disable it.

## power settings
Disable screen turning off -> xfce4-power-manager-settings

## imagej
https://fiji.sc/#download

## p4merge
http://blogs.pdmlab.com/alexander.zeitler/articles/installing-and-configuring-p4merge-for-git-on-ubuntu/

## tobii certificate
https://artifactory.tobii.intra/artifactory/tt-toolchains/certs/Tobii-CA-ROOT-SHA256-current.cer
